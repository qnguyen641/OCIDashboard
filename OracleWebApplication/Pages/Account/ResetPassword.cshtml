@page
@model OracleWebApplication.Pages.Account.ResetPasswordModel
@{
    ViewData["Title"] = L["ResetPw_Title"];
}

<div class="row justify-content-center">
    <div class="col-md-5">
        <h1>@ViewData["Title"]</h1>
        <p class="text-muted">@L["ResetPw_Desc"]</p>
        <hr />
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
            <input asp-for="Input.Token" type="hidden" />
            <div class="mb-3">
                <label asp-for="Input.Email" class="form-label">@L["Login_Email"]</label>
                <input asp-for="Input.Email" class="form-control" autocomplete="email" readonly />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label asp-for="Input.Password" class="form-label">@L["ResetPw_NewPassword"]</label>
                <input asp-for="Input.Password" class="form-control" autocomplete="new-password" />
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label asp-for="Input.ConfirmPassword" class="form-label">@L["ResetPw_ConfirmPassword"]</label>
                <input asp-for="Input.ConfirmPassword" class="form-control" autocomplete="new-password" />
                <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary w-100">@L["ResetPw_Submit"]</button>
        </form>
        <div class="mt-3 text-center">
            <a asp-page="/Account/Login">@L["ForgotPw_BackToLogin"]</a>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
